# FreqGov config (source'd by freqgov.sh)
# boost window ms (hard cap 5000)
BOOST_MS=2000
# scroll clamp for policy4 max, Hz; 0 = disable
SCROLL_MAX=900000
# scroll hold after last touch, ticks (100ms each)
HOLD_TICKS=4
